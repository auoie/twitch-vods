datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model streams {
  id              String   @id @default(uuid())
  streamer_id     String
  stream_id       String   @unique
  start_time      DateTime
  max_views       Int
  last_updated_at DateTime
  time_series     Bytes
}
