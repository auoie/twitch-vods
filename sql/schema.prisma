datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Stream {
  id            String   @id @default(uuid())
  streamerId    String
  streamId      String   @unique
  startTime     DateTime
  maxViews      Int
  lastUpdatedAt DateTime
  timeSeries    Bytes
}
